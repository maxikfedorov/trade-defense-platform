server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://db:5432/appdb}
    username: ${DB_USER:appuser}
    password: ${DB_PASSWORD:apppass}

ai:
  baseUrl: ${AI_BASEURL:http://localhost:8000}
